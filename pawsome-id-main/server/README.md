
# Pawsome Backend

Simple Node.js + Express backend for the Pawsome React frontend.

Quick start

1. cd server
2. npm install
3. copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`
4. npm run dev

API base: `/api`
